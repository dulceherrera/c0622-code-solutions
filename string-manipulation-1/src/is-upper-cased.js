/* exported isUpperCased */
/* Return the input word to check if all the characters of the word are strictly equal to uppercase */
function isUpperCased(word) {
  return word === word.toUpperCase();
}
