/* exported isLowerCased */
/* Return the input word to check if all the characters of the word are strictly equal to lowercase */
function isLowerCased(word) {
  return word === word.toLowerCase();
}
